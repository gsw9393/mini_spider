;[spider]
# 种子文件路径
urlListFile = ../data/url.data

# 抓取结构存储目录
outputDirectory = ../output

# 抓取最大深度(种子为0级)
maxDepth = 1

# 抓取间隔,单位:秒
crawInterval = 1

# 抓取超时,单位:秒
crawTimeout = 1

# 需要存储的目标网页URL pattern(正则表达式)
targetUrl = .*.(htm|html)$

# 抓取routine数目
threadCount = 8
